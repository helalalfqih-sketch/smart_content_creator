# الكود المهجور والملفات اليتيمة — Dead & Unreachable Code
## مشروع Smart Content Creator

---

## 1. الملفات غير المستخدمة واليتيمة داخل مسار المشروع (Orphaned Files)

تم اكتشاف عدة ملفات متبقية من جلسات تطوير وتجارب سابقة داخل مجلد `lib/`:

| # | مسار الملف (File Path) | الحجم | الحالة والتصنيف | الإجراء الموصى به |
|---|---|---|---|---|
| 1 | `lib/screens/Untitled-1.txt` | **335 KB** | 🔴 ملف نصي يحتوي مقتطفات ومسودات كود قديمة غير مستخدمة داخل مجلد الـ screens | الحذف فوراً بعد التأكد من عدم وجود بيانات مفقودة |
| 2 | `lib/screens/ai_chat_screen.dart.tmp` | **37 KB** | 🟠 ملف مؤقت لنسخة سابقة من شاشة الدردشة | الحذف |
| 3 | مسار Codebase في `firebase.json`: `"source": "backend"` | سطر إعداد | 🔴 يشير لمجلد `backend/` غير موجود بالمشروع، مما يسبب فشل النشر التلقائي للـ Functions | إزالة الإعداد أو إنشاء المجلد |

---

## 2. المسارات والدوال غير المستدعاة (Unused Methods & Routes)

1. **المسار المكرر المهجور:**
   - المسار `/main` في `lib/main.dart` مكرر مع `/home` ولا يتم استدعاؤه في أي زر انتقال داخلي (كل التنقل يتم عبر `/home`).
2. **المتغيرات والواردات غير المستخدمة:**
   - تم رصد وجود دوال معالجة قديمة داخل `CatalogProduct` للتحويل من هياكل Firestore السابقة التي تم استبدالها بـ Back4App.
3. **أصول وملفات نصية في جذر المشروع:**
   - `app_check_debug.txt` و `build_error.txt` — ملفات سجلات ناتجة عن تشغيلات سابقة لا ينبغي تضمينها في الإنتاج.

---

## 3. خطة التنظيف الآمن (Safe Cleanup Checklist)

- [ ] حذف `lib/screens/Untitled-1.txt`
- [ ] حذف `lib/screens/ai_chat_screen.dart.tmp`
- [ ] إزالة مرجع `"source": "backend"` من `firebase.json`
- [ ] إزالة المسار غير المستخدم `/main` من جدول مسارات `GetPage`
- [ ] إضافة مسارات الملفات المؤقتة وسجلات الأخطاء لملف `.gitignore`
