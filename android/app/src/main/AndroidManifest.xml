<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Storage permissions for saving to gallery -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <application
        android:label="صانع المحتوى "
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- Deep Linking for Firebase Auth & TikTok Callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:host="smartcontentcreator.page.link"
                    android:scheme="https"/>
                <data
                    android:host="smartcc.ai"
                    android:scheme="https"/>
                <data
                    android:host="smartcontentcreator-d49f2.web.app"
                    android:scheme="https"
                    android:pathPrefix="/auth/tiktok/callback"/>
            </intent-filter>

            <!-- Receive Share Intents -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Android App Links (Auto-Verify) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="smartcontentcreator-d49f2.web.app"
                    android:pathPrefix="/" />
                <data
                    android:scheme="https"
                    android:host="smartcontentcreator-d49f2.firebaseapp.com"
                    android:pathPrefix="/" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <!-- ✅ HTTP/HTTPS - متصفح الويب -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>

        <!-- 🎵 TikTok (Global) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tiktok" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="snssdk1233" />
        </intent>

        <!-- 🎵 Douyin (China TikTok) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="douyin" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="snssdk1128" />
        </intent>

        <!-- ⚡ Kuaishou -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="kwai" />
        </intent>

        <!-- 📕 Rednote / XiaoHongShu -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="xhsdiscover" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="xhs" />
        </intent>

        <!-- 📺 Bilibili -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="bilibili" />
        </intent>

        <!-- 🛍️ Taobao -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="taobao" />
        </intent>
        <!-- Package visibility لـ Taobao (Android 11+) -->
        <package android:name="com.taobao.taobao" />

        <!-- 🛒 JD.com -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="openapp.jdmobile" />
        </intent>
        <package android:name="com.jingdong.app.mall" />

        <!-- 💬 WhatsApp -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="whatsapp" />
        </intent>
        <package android:name="com.whatsapp" />

        <!-- 📸 Instagram -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="instagram" />
        </intent>

        <!-- 🎬 YouTube -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="youtube" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="vnd.youtube" />
        </intent>

        <!-- 📦 Package visibility للمنصات الصينية الرئيسية -->
        <package android:name="com.ss.android.ugc.aweme" />       <!-- Douyin -->
        <package android:name="com.smile.gifmaker" />              <!-- Kuaishou -->
        <package android:name="com.xingin.xhs" />                  <!-- Rednote -->
        <package android:name="tv.danmaku.bili" />                 <!-- Bilibili -->
        <package android:name="com.zhiliaoapp.musically" />        <!-- TikTok -->

        <!-- 🔤 Process Text (Flutter default) -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
